# -----------------------------------------------------------------------
# Missions Framework — coding-agent
# -----------------------------------------------------------------------

# -----------------------------------------------------------------------
# Core LLM / API client
# -----------------------------------------------------------------------
openai>=1.30.0

# -----------------------------------------------------------------------
# Phase 1 — Telemetry (Arize Phoenix)
# -----------------------------------------------------------------------
arize-phoenix>=4.0.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
openinference-instrumentation-openai>=0.1.12

# -----------------------------------------------------------------------
# Phase 2 — Dynamic Tool Routing
#   Primary encoder  : BAAI/bge-base-en-v1.5 (768-dim, local, via sentence-transformers)
#   Fallback encoder : OpenAI text-embedding-3-small (768-dim, cloud)
#   Vector store     : Qdrant Cloud (qdrant-client with HTTP API)
# -----------------------------------------------------------------------
qdrant-client>=1.9.0
sentence-transformers>=3.0.0

# -----------------------------------------------------------------------
# Phase 4 — Memory Layer (Cognee knowledge graph)
# -----------------------------------------------------------------------
cognee>=0.1.15

# -----------------------------------------------------------------------
# Agentic pipeline (LangGraph — kept for benchmark compatibility)
# -----------------------------------------------------------------------
langgraph>=0.2.0
langchain-core>=0.2.0

# -----------------------------------------------------------------------
# Data & stats (benchmark analysis)
# -----------------------------------------------------------------------
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# -----------------------------------------------------------------------
# Visualization (benchmark charts)
# -----------------------------------------------------------------------
matplotlib>=3.9.0

# -----------------------------------------------------------------------
# Code quality (used by run_linter tool)
# -----------------------------------------------------------------------
flake8>=7.0.0
black>=24.0.0

# -----------------------------------------------------------------------
# Testing (used by run_pytest tool)
# -----------------------------------------------------------------------
pytest>=8.0.0

# -----------------------------------------------------------------------
# Model downloads
# -----------------------------------------------------------------------
huggingface_hub>=0.23.0

# -----------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------
tqdm>=4.66.0
python-dotenv>=1.0.0
json-repair>=0.30.0

# -----------------------------------------------------------------------
# Phase 3 — FastAPI Control API
# -----------------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9