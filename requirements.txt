# =============================================================================
# Requirements for Qwen 3.6 MTP Speculative Decoding on RTX 4090 (24GB)
# =============================================================================
# GPU: NVIDIA RTX 4090 - Compute Capability 8.9 (sm89)
# CUDA: 12.5+ 
# VRAM: 24GB
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0

# vLLM 0.19.1 with CUDA 12.4+ support
vllm==0.19.1

# AWQ quantization support (for pre-quantized models)
autoawq>=0.2.7

# FlashAttention 2 - RTX 4090 supports this!
flash-attn>=2.6.0

# Model Support
transformers @ git+https://github.com/huggingface/transformers.git
accelerate==1.2.0
huggingface_hub>=0.25.0
safetensors>=0.4.0

# xFormers (optional but good for older GPU compatibility fallback)
xformers==0.0.29.post1

# Agentic Pipeline
langgraph>=0.2.0
openai>=2.0.0
langchain-core>=0.2.0

# Data & Stats
pandas>=2.2.0
numpy>=1.26.0,<2.0.0
scipy>=1.13.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
matplotlib>=3.9.0
sentencepiece>=0.1.99
protobuf>=3.20.0